name: Install Zola

on:
  workflow_call:

env:
  ZOLA_VERSION: 0.20.0

jobs:
  install:
    runs-on: ubuntu-latest

    steps:
      - name: Install Zola
        uses: taiki-e/install-action@v2
        with:
          tool: zola@${{ env.ZOLA_VERSION }}
